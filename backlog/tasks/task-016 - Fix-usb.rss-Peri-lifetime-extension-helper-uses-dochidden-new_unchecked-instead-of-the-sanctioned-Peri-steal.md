---
id: TASK-016
title: >-
  Fix: usb.rs's Peri lifetime-extension helper uses #[doc(hidden)] new_unchecked
  instead of the sanctioned Peri::steal()
status: Needs Plan
assignee: []
created_date: '2026-08-01 22:51'
updated_date: '2026-08-01 23:16'
labels:
  - review-followup
  - planned
dependencies:
  - TASK-014
priority: high
type: bug
ordinal: 90
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Found while reviewing TASK-014 (crates/asperitas-logging/src/usb.rs:73-86, `extend_peri_to_static`). TASK-014's stated goal was to replace `core::mem::transmute` with "embassy-hal-internal's own sanctioned unsafe primitive" for extending a `Peri<'_, T>` borrow to `'static`. It landed on `Peri::new_unchecked(*p)`. But `Peri::new_unchecked` in embassy-hal-internal-0.5.0/src/peripheral.rs:22-34 is marked `#[doc(hidden)]` and its own doc comment says explicitly: "For use by HALs only. If you're an end user you shouldn't use this, you should use `steal()` on the actual peripheral types instead." asperitas-logging is not a HAL — it is exactly the "end user" this comment is warning off. The crate does provide the actual sanctioned end-user API for this: each concrete peripheral type gets an inherent `pub const unsafe fn steal() -> Peri<'static, Self>` generated by the `peripherals_definition!` macro (embassy-hal-internal-0.5.0/src/macros.rs:15-25), specifically for conjuring a `'static` Peri outside HAL-internal code. TASK-014 satisfied its acceptance criteria literally (no `transmute`, builds, clippy-clean) but did not satisfy the ticket's actual Resilient-axis intent: "unsafe code that doesn't need to be as unsafe as it is." It now depends on a `#[doc(hidden)]` item that is not part of embassy-hal-internal's public contract (no semver guarantee it keeps working, even though it happens to be technically `pub`), instead of the item the crate's own docs point end users to. Note this is not a one-line swap: `steal()` is an inherent function generated per concrete peripheral type, not a method on the `PeripheralType` trait `extend_peri_to_static<T: hal::PeripheralType>` is generic over, so it can't be called generically through that bound without further design work (see plan).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 grep -rn 'new_unchecked' crates/asperitas-logging/src returns no results (or, if kept, a code comment justifies why the doc(hidden)/HAL-only primitive is used over steal() — see plan step 3 for when that's the correct outcome)
- [ ] #2 The chosen replacement is documented once with the same safety invariant asperitas-logging/src/usb.rs's current extend_peri_to_static doc comment states (peripherals never dropped for device lifetime; only one live Peri constructed), not duplicated at each of the three call sites
- [ ] #3 cd firmware && nix develop /home/jeffutter/src/asperitas -c cargo build --release --features seed3 --bin main --bin blinky succeeds
- [ ] #4 cd firmware && nix develop /home/jeffutter/src/asperitas -c cargo clippy --release --features seed3 --bin main --bin blinky -- -D warnings passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Option (a): drop the generic extend_peri_to_static helper; call T::steal() directly at each of the three sites.

## Changes to crates/asperitas-logging/src/usb.rs

### 1. Remove the helper (lines 69-86)
Delete the entire "Lifetime extension helper" section:
- The module-level comment '// --- Lifetime extension helper ---'
- The doc comment on extend_peri_to_static
- The extend_peri_to_static<T: hal::PeripheralType> function itself

### 2. Replace the three call sites in init() (lines 119-121)
Replace the three extend_peri_to_static calls with direct steal() calls, preceded by a SINGLE safety comment block:

    // Safety: peripherals are never dropped for the life of the device, and init()
    // holds the only Peri constructed for each of these peripherals. Calling steal()
    // here discards the borrowed Peri<'_, T> parameter and conjures a fresh 'static
    // one - no two live drivers will ever exist simultaneously.
    let usb_otg_fs = unsafe { hal::peripherals::USB_OTG_FS::steal() };
    let dp = unsafe { hal::peripherals::PA12::steal() };
    let dn = unsafe { hal::peripherals::PA11::steal() };

Each steal() call gets its own minimal unsafe block. The safety invariant is stated ONCE in the comment above, not repeated per line. This follows embassy-hal-internal's explicit guidance to end users (Peri::new_unchecked docs say "you should use steal() on the actual peripheral types instead").

### 3. Verify
- grep -rn 'new_unchecked' crates/asperitas-logging/src -> no results
- cd firmware && nix develop /home/jeffutter/src/asperitas -c cargo build --release --features seed3 --bin main --bin blinky
- cd firmware && nix develop /home/jeffutter/src/asperitas -c cargo clippy --release --features seed3 --bin main --bin blinky -- -D warnings
<!-- SECTION:PLAN:END -->
